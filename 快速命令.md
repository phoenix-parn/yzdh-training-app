# 快速命令参考

## 🔧 每次打开新终端时

### 刷新环境变量(让Git可用)
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

---

## 📝 Git常用命令

### 查看状态
```bash
git status
```

### 添加修改
```bash
git add .
```

### 提交代码
```bash
git commit -m "描述你的修改"
```

### 推送到GitHub
```bash
git push
```

### 一键推送(使用脚本)
```powershell
.\git-push.ps1
```

---

## 🚀 完整更新流程

```powershell
# 1. 刷新环境变量
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

# 2. 查看修改
git status

# 3. 添加所有修改
git add .

# 4. 提交
git commit -m "Update: 描述修改内容"

# 5. 推送
git push
```

---

## 🌐 网络问题解决

如果推送失败显示连接超时:

### 方法1: 重试
```bash
git push
```

### 方法2: 检查网络
- 确认可以访问 github.com
- 尝试使用VPN或代理

### 方法3: 使用SSH(推荐)
```bash
# 1. 生成SSH密钥
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 添加到GitHub: Settings → SSH and GPG keys

# 3. 修改远程仓库URL
git remote set-url origin git@github.com:phoenix-parn/yzdh-training-app.git

# 4. 推送
git push
```

---

## 📦 开发常用命令

### 启动开发服务器
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 同步到Android
```bash
npx cap sync android
```

### 打开Android Studio
```bash
npx cap open android
```

---

## 🔍 查看构建状态

访问: https://github.com/phoenix-parn/yzdh-training-app/actions

---

## 💡 提示

- 每次打开新的PowerShell都需要刷新环境变量
- 或者关闭并重新打开PowerShell
- 推荐使用 `git-push.ps1` 脚本简化操作

---

**保存此文件以便快速查阅!**
